# SoundCeiling v0.7.3 Samsung Corrective Field Checklist

Status: **awaiting device test** on Samsung SM-A528B.

## Automated evidence
- [ ] CI pure suite and historical/current contracts GREEN on final APK commit.
- [ ] Android `:app:assembleDebug` GREEN on final APK commit.
- [ ] APK SHA-256 recorded from the exact CI artifact.

## User authority / Samsung Media
- [ ] Manually lower Samsung Media while SoundCeiling is running. The app never raises Media above the newly chosen user step.
- [ ] Repeat while a previous SoundCeiling Media-UP write is pending. A mismatch is treated as user authority, not app ownership.
- [ ] SoundCeiling-owned attenuation may later repay only its own debt, and only back to the latest user anchor.
- [ ] Hard safety may still lower Media below the anchor when genuinely required.

## Capture rebind lifecycle
- [ ] YouTube/Yandex source changes between mixed and targeted PCM do not reset the Samsung user anchor.
- [ ] Capture rebind does not destroy a route-scoped Global DSP transport/proof.
- [ ] An in-flight -2 dB DSP probe is neutralized before its measurement source is replaced.

## Source recognition
- [ ] Missing Notification Listener access shows **«Разрешить распознавание источника для DSP»**.
- [ ] YouTube and Yandex Music candidates/target probe outcomes are still logged truthfully.

## Global DSP
- [ ] Log contains `global_dsp_transport` after the persistent session logger is attached.
- [ ] If custom DynamicsProcessing config fails, detail identifies the default-config fallback attempt/result.
- [ ] If transport is available, `global_dsp_probe` records meter and measured delta.
- [ ] `VERIFIED_GLOBAL_MIX` / ACTIVE appears only after measured proof; otherwise the app remains unavailable/unverified and does not claim DSP control.

## Artifact
APK source commit: awaiting final CI
GitHub Actions run: awaiting final CI
Artifact: `SoundCeiling-v0.7.3-debug-apk`
APK SHA-256: awaiting final CI
