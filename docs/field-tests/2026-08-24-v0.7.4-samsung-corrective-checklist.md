# SoundCeiling v0.7.4 Samsung Corrective Field Checklist

Status: **awaiting device test** on Samsung SM-A528B.

This field build is driven by `SoundCeiling-20260824-003430.log.txt` from v0.7.3. Automated tests can prove ordering and invariants, but only this physical test can show whether Samsung's session-zero `DynamicsProcessing` actually changes the output mix.

## Automated evidence
- [ ] Final v0.7.4 CI pure suite and historical/current contracts GREEN.
- [ ] v0.7.4 corrective contract GREEN.
- [ ] v0.7.4 release contract GREEN.
- [ ] Android `:app:assembleDebug` GREEN on the exact final APK commit.
- [ ] APK SHA-256 recorded from the exact CI artifact.

## Global DSP probe, primary v0.7.4 acceptance
- [ ] Log contains `global_dsp_transport` and identifies custom-config result plus `default_config_fallback` when Samsung rejects the custom config.
- [ ] With stable Yandex Music or YouTube playback, `global_dsp_probe` starts on `meter=OUTPUT_MIX`.
- [ ] While the bounded -2 dB probe is being measured, ordinary coordinator control reports `global_dsp_probe_measurement_hold` instead of immediately issuing `dsp_capability_lost_neutralize`.
- [ ] A completed probe reports a real measured delta and is not contaminated by a mixed↔targeted rebind.
- [ ] If a rebind interrupts the probe, the old measurement cannot become a new verified route proof; a later fresh probe is used instead.
- [ ] `VERIFIED_GLOBAL_MIX` / ACTIVE appears only if the measured proof really passes.
- [ ] If verified, ordinary quiet/loud normalization uses DSP gain and Samsung Media remains at the user's chosen anchor except for independent hard safety.
- [ ] If still unverified, the log keeps `AVAILABLE_UNVERIFIED`/fallback honestly and never invents DSP success.

## Capture reference
- [ ] During one stable audible targeted-capture interval, make at least three clear manual Samsung Media step changes so PRE/POST evidence can be measured. Do not perform these changes during silence/pause.
- [ ] Log records `capture_reference ... evidence=3` if all three samples are consistent before the next capture replacement.
- [ ] Silent Media changes do not advance useful capture-reference evidence.
- [ ] mixed↔targeted capture replacement resets PRE/POST estimator evidence because that evidence is capture-dependent; it must not reset Samsung user anchor/debt or route-scoped Global DSP state.
- [ ] UNKNOWN remains fail-closed for ordinary normalization writes that cannot be projected safely.

## User authority / Media debt
- [ ] Manually lower Samsung Media while SoundCeiling is running. Ordinary normalization never raises Media above that newly chosen step.
- [ ] An app-write mismatch is treated as external/user authority.
- [ ] SoundCeiling may repay only its own attenuation debt and only up to the latest user anchor.
- [ ] No fast 4→3→4 style fight loop reappears.

## Safety
- [ ] Hard Media cap still applies even if a DSP probe is in flight: probe attenuation is neutralized first, then the hard-cap Media command follows.
- [ ] Quiet Now can interrupt an in-flight probe only through neutralization first.
- [ ] Hard peak protection and emergency downward attenuation remain available.

## Source recognition / rebind
- [ ] Missing Notification Listener access still shows **«Разрешить распознавание источника для DSP»**.
- [ ] Yandex Music and YouTube become EXACT only after stable UID-targeted PCM, not from package candidate alone.
- [ ] mixed↔targeted capture rebind does not reset Samsung user anchor, linked ceilings or app-owned attenuation debt.

## Product result
- [ ] Quiet and loud sections audibly converge more than in v0.7.3 once a valid actuator path is available.
- [ ] Normalizer can remain enabled for an extended playback session without obvious oscillation or slider fighting.
- [ ] System-sound behavior matches the actually active Global DSP/fallback state and the UI does not claim selective isolation on an indivisible verified global mix.

## Artifact
APK source commit: awaiting final CI
GitHub Actions run: awaiting final CI
Artifact: `SoundCeiling-v0.7.4-debug-apk`
APK SHA-256: awaiting final CI
