# SoundCeiling v0.7.2 Samsung Corrective Field Checklist

Status: **awaiting device test** on Samsung SM-A528B.

## Automated evidence
- [ ] CI pure suite and historical/current contracts GREEN on final APK commit.
- [ ] Android `:app:assembleDebug` GREEN on final APK commit.
- [ ] APK SHA-256 recorded from the exact CI artifact.

## Master anchor / fallback
- [ ] Start at Samsung Media 8/15. SoundCeiling does not drag the master toward 1/15 during ordinary playback.
- [ ] Move Samsung Media manually while running. The new step becomes the user anchor and is not immediately fought by the app.
- [ ] App-owned attenuation may move below the anchor when needed, then returns only toward the anchor when the loud section ends.
- [ ] Ordinary auto floor is route-relative; emergency hard safety may still go below it when genuinely required.

## Capture reference and normalization
- [ ] Logs show `capture_reference` moving from UNKNOWN to PRE_VOLUME or POST_VOLUME from measured evidence.
- [ ] On this Samsung, near-0 dBFS Playback Capture at low Media no longer causes a false physical hard-peak cascade.
- [ ] Quiet -> loud -> quiet material returns toward the same perceived output without permanent low-volume state.
- [ ] 20 dB quiet/loud test records reaction, reversal and recovery without a 1->2->1 fight loop.

## Source recognition
- [ ] If Notification Listener access is missing, Simple shows the permission button and logs `media_session_access=missing`.
- [ ] YouTube candidate is logged with package/UID and targeted PCM result.
- [ ] Yandex Music candidate is logged with package/UID and targeted PCM result.
- [ ] Silent/failed target is labeled explicitly, not collapsed to generic UNKNOWN.

## Global DSP
- [ ] With Global DSP ON, log contains `global_dsp_transport` with raw creation capability.
- [ ] If transport is available, bounded `global_dsp_probe` starts and records meter source plus measured delta.
- [ ] `VERIFIED_GLOBAL_MIX` / ACTIVE appears only after real proof; otherwise status remains UNAVAILABLE or AVAILABLE_UNVERIFIED.
- [ ] Capability loss neutralizes DSP before Media fallback.

## Linked Lock / UI
- [ ] Default Linked Lock can be toggled while engine is running and does not snap back from stale service state.
- [ ] Linked ON keeps both ceiling sliders visible but disabled/dimmed in Simple and Advanced.
- [ ] `Вернуть настройки по умолчанию` restores normalizer defaults but preserves logs, calibration and app rules.
- [ ] `Частотный спектр` is understandable as diagnostic-only and does not imply it controls normalization.

## Artifact
APK source commit: awaiting final CI
GitHub Actions run: awaiting final CI
Artifact: `SoundCeiling-v0.7.2-debug-apk`
APK SHA-256: awaiting final CI
